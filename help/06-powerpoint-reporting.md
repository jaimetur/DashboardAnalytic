# PowerPoint Reporting

The PowerPoint export generates a `.pptx` report with:

- title slide
- KPI summary slide
- scorecard slide

A future iteration can replace the current generic layout with a branded template and chart rendering.
