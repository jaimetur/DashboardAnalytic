# Administration Panel

The administration panel includes:

- user creation
- dataset listing
- audit log inspection

The current implementation uses SQLite for persistence and is intended as a lightweight operational control surface.
