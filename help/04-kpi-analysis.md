# KPI Analysis

The analytics layer currently provides:

- dataset filtering by market and period when those columns exist
- metric selection over numeric columns
- KPI summary cards
- percentile scorecard
- CDF computation and visualization payload
- top record extraction for table rendering

This is the extension point for the future NetCheck scoring and GAP logic.
