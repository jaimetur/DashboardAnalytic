# Runtime Arguments Summary

This project is primarily configured through environment variables rather than CLI flags.

Short summary:

- Use `APP_PORT` for production container port mapping.
- Use `APP_DEV_PORT` for development container port mapping.
- Use `APP_*_DIR` values to control storage locations.
