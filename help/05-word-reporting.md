# Word Reporting

The Word export generates a `.docx` report with:

- selected metric
- filtered row count
- KPI summary
- scorecard values

A future iteration can switch this implementation to a branded corporate template.
